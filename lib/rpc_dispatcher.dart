/// It's just an interface for method invocation
/// in a JSON-RPC implementation. There are two
/// implementations that don't look the same, and one
/// is incompatible with flutter.
library rpc_dispatcher;

export 'src/rpc_dispatcher_base.dart';
